public class Main {
  public static void main(String[] args) {
      int index;

      // Reading

      FileIO.reader();

      // Logic

      Game.deck(); 
    
      index = SignIn.start();
  
      // if (index == -1) {
      //     System.exit(0);
      // } else {
      //   Game.setIndex(index);
      // }

      // Writing

      FileIO.writer();
        
      
    }
}
